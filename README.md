# strutils

A tiny collection of string utility functions, used as a practice repo for learning Git and GitHub workflows.

## Functions

- `is_palindrome(s)` — returns `True` if the string reads the same forwards and backwards (ignoring case and spaces).
- `word_count(s)` — returns the number of words in a string.

## Running tests

```bash
python -m unittest discover tests
```
